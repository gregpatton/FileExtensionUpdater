FileExtensionUpdater
====================
VB.NET WinForm application that updates the file extension of every file in a provided directory.  The user sets the input directory as well as the file extension to be used.
